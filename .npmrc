# Ensure platform-specific optional dependencies (notably sharp's
# @img/sharp-<platform>-<arch> binaries) are installed even when the host
# OS differs from the deploy target. Without this, a lockfile generated
# on Windows omits @img/sharp-linux-x64, breaking the Vercel build.
include=optional
